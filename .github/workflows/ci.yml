name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  check:
    name: fmt · vet · test · build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-go@v7
        with:
          go-version: stable
          cache: true

      - name: Formatting
        run: |
          unformatted=$(gofmt -l .)
          if [ -n "$unformatted" ]; then
            echo "These files need gofmt:"
            echo "$unformatted"
            exit 1
          fi

      - name: Vet
        run: go vet ./...

      - name: Tests
        run: go test ./... -race

      # The seed corpus runs above with every other test. This is a short
      # exploratory run on top, so a change that lets a capsule.toml reach argv
      # in a shape it should not is caught here rather than by a user.
      - name: Fuzz the config reader
        run: go test ./internal/runtime -run FuzzParseToArgv -fuzz FuzzParseToArgv -fuzztime 60s

      - name: Build
        run: go build -o capsule ./cmd/capsule

      - name: CLI smoke test
        run: |
          ./capsule version
          ./capsule --help
          # ubuntu-latest has Docker, so doctor should find a healthy runtime
          # and a valid capsule.toml in this repo.
          ./capsule doctor
          ./capsule up --dry-run

  image:
    name: docker build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Build the image
        run: docker build -t capsule:ci .

      - name: Run it
        run: docker run --rm capsule:ci version
